#Constraint condition
#W_S_default = seq(from = 10,
   #       to = 50,
    #      by = .1)#Range of Wing Loading(x axis to be plotted) on the graph
#

#CA_default<-list(Cd_min=.01,#Minimum drag coefficient
           #      q = 1/2*1.24*40^2,#    value of q (potential energy density)
           #      n = 1,#                number of engines
            #     k = 1/50,#            1/R
            #     Ps = 5000,#            energy level
            #     v=40,
            #     rho = 1.225,#          density of air https://en.wikipedia.org/wiki/Density_of_air
            #     mew = 1.0#     friction coefficient rubber on concrete https://hypertextbook.com/facts/2006/MatthewMichaels.shtml
               #  )
#Specify by CA<-CA_...(e.g. CA<-CA_default)
